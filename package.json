{
  "name": "lead-finder",
  "version": "0.1.0",
  "description": "Find local businesses without websites via OpenStreetMap. Zero-cost lead gen for web designers.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  }
}
