# No env vars are required to run Lead Finder anymore — every visitor enters
# their own SerpAPI key (stored in their browser's localStorage).
#
# This file is kept only for the standalone CLI test in backend/serpapi.js.
# If you want to run that test (`node backend/serpapi.js`), put your own key here:
SERPAPI_API_KEY=
